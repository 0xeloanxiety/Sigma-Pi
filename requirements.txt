# --- schema / contract ---
pydantic>=2,<3

# --- Solidity parsing (tree-sitter, no compiling) ---
tree-sitter>=0.21
tree-sitter-language-pack>=0.2

# --- static-analysis detector  ---
slither-analyzer>=0.10
solc-select>=1.0

# --- data prep (auditor/train/prepare_data.py) ---
pandas>=2
scikit-learn>=1.3
pyyaml>=6

# --- training: actually runs on Colab, installed here ONLY to
#     verify it works on Python 3.11 ---
torch>=2.2
transformers>=4.40
datasets>=2.19

# --- tests ---
pytest>=8